Feature: Redirection to https
  It has been decided that all requests get redirected through to HTTPS.
  This is simpler than serving some pages as this and some as that, also, the user can sign-in so it makes sense that https is always used anyway.